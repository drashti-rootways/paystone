query CartInput {
  cart {
    attributes {
      key
      value
    }

    cost {
      totalAmount {
        amount
      }
    }

    lines {
      id
    }
  }

  discount {
    discountClasses
  }
}