query CartInput {
  cart {
    attribute(key: "paystoneBalance") {
      value
    }

    cost {
      totalAmount {
        amount
      }
    }

    lines {
      id
    }
  }

  discount {
    discountClasses
  }
}